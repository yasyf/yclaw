# @@AGENT_VAULT_CA_PEM@@ — placeholder so ./agent-vault-ca.pem resolves; the deploy flow overwrites it.
# The deploy flow fetches GET http://metal.@@TAILNET_DOMAIN@@:14321/v1/mitm/ca.pem into this file and
# commits the PUBLIC PEM (a CA public cert is not a secret). Until then this is a non-PEM placeholder;
# `nix flake check` reads the path literally and security.pki.certificateFiles installs it into the OS
# trust store on the hermes VM. Replace this entire file with the real -----BEGIN CERTIFICATE----- block.
