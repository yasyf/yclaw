{"v":1,"lamport":3,"ops":[{"kind":"append_entry","text":"`gh run list --commit \u003cshort-sha\u003e` silently returns [] — it filters by exact headSha string match, so a 12-char abbreviated SHA never matches and looks like \"no CI runs\". Cost a 10-minute watcher timeout on the host-pf landing. Always `git rev-parse` to the full 40-char SHA before passing --commit."}]}