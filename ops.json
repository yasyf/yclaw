{"v":1,"lamport":2,"ops":[{"kind":"add_comment","body":"This is **shipped** on main as `f7b7038` (\"refactor(build): retire legacy VM-disk image machinery; one container build path\") — the diff matches this task's scope verbatim (deletes build-hermes-image.sh + build-images.yml + nixos/image.nix + scratch-login.nix + deploy-vm.sh; edits flake.nix/justfile/bootstrap.sh/common.nix; the genericity guard is ported into `scripts/build-container-image.sh`, verified present). Criteria 1–2 (no legacy refs; single build path + guard) met. Criterion 3 (flake eval + container build + pytest green) not re-run this session — recommend the owner close after a verification pass. Not force-closing from acct-14. (Migration status: task f631156 comment.)"}]}