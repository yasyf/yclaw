{"v":1,"lamport":10,"ops":[{"kind":"append_entry","text":"Durable attachment: the verified destination-screening fix as a unified diff against `pkgs/hermes-docker-proxy/policy.go` (adds `Target`/`Destination` to the mount struct, parses the Binds destination + Mounts Target/Destination, and a `forbiddenDest()` that rejects any bind/mount whose destination basename is `docker.sock`). This is the exact patch that closed the socktainer docker.sock-relay host-escape bypass (verified: exploit → 403 both `-v` and `--mount` forms; legit bind still works; `go test` green). Port target for backlog task da1c63."},{"kind":"add_attachment","name":"proxy-destination-screen.patch","oid":"262fc158affecd22f4595b70ecd0bb7f9d1b076ec247ea837b5197a5de0f0cc5","size":2460}]}