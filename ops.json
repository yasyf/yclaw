{"v":1,"lamport":3,"ops":[{"kind":"add_comment","body":"**Live ACL `tag:vault` push: ALREADY APPLIED — verified, no edit made.** Fetched the live tailnet ACL (login.tailscale.com/admin/api/acls, hash `4bd89aa0…`) via cookiesync→curl and audited it against `tailnet/policy.hujson`. All `tag:vault` bits are already present and byte-consistent: tagOwners `tag:vault` (autogroup:admin+self), ssh rule dst includes `tag:vault`, and all 3 grants (hermes→vault tcp:8317/14321/14322/8000/8765; vault→yasyf-home tcp:8000/8765; yasyf-home→vault tcp:14321/8317/8000/8765). `yasyf-home` host present. No over-exposure: `tag:vault` is reachable only via those explicit grants (the autogroup:member→*:* acl doesn't cover tagged nodes). So this P3 prerequisite is **satisfied** — a prior session landed it. Remaining P3 blockers: OAuth client `tag:vault` widen (user console; worth verifying it's not also already done) + reconcile session minting vault's bundle."}]}